alert('Com a nossa mensagem!'); // Dispara um alerta na tela

const confirma = confirm('Realmente deseja apagar?'); // Armazena o true ou false na variável confirma

let num1 = prompt('Digite um número!'); // Solicita que o usuário escreva um número no campo input e armazena na variável num1
num1 = parseFloat(num1)